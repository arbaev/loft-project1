var addModule = (function() {

	var init = function () {
		// инициализация функций
		_setupListeners();
	};

	var _setupListeners = function () {
		// прослушка событий
	}

	return {
		init : init
	};
})();

addModule.init();